1. request dto를 서비스로 가져갈 때, 새로운 dto를 만들어야 하는데 이름은..?
(entity이름과 동일하게 해버리면 이름이 겹쳐버린다)

2. 테이블 fk 설정 줄지 말지

3. admin 모듈 내의 패키지 명을 어떻게 두는게 좋을지


2. 특정 필드 제외한 생성자(빌더)로 객체 생성 시, 나머지 필드에는 어떤 값이 들어가는지
- null로 들어간다
- (즉, 필드 자체에 값이 없는 것이 아니다. null이라는 값이 들어간다.
따라서 mysql default를 통해 createdAt, updateAt을 지정하려고 하면 안된다.
default는 아예 필드에 값이 없을 경우 활성화되기 때문이다.)


가상주소가 16KB 인 이유
메모리 크기32bit로 한 이유
- 명령어 크기 32비트
- 나머지가 64비트


학생으로 다가기는건 안좋아한다..
토이 프로젝트

프로젝트 - 출시를 목적으로 하는 것...
회사에서 배우는게 나을 것 같음...
솔로로 읽으면 안되는 것 같고...